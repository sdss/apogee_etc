Installation
============

Clone the repository::

    git clone https://github.com/sdss/apogee_etc.git
    cd apogee_etc

Install::

    pip install -e .

Install with Streamlit support::

    pip install -e ".[app]"

Install development dependencies::

    pip install -e ".[app,test]"

